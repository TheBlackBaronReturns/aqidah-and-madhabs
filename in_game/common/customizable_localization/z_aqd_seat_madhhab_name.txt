# =====================================================================
# AQIDAH SCHOOLS OF CREED - dynamic "which madhhab is this seat" text.
#
# Modeled on the confirmed base-game type = location pattern (this same
# folder's locations.txt derry/london entries) and the confirmed type =
# country inline-embed pattern (customizable_localization/estates.txt's
# crown_estate, invoked elsewhere as literal $crown_estate$ in loc text -
# e.g. main_menu/localization/english/actions_l_english.yml's
# ruler_regent_relatives_crown_estate_tt). Every current and future fatwa
# event naming a seat's madhhab should reference $aqd_seat_madhhab_name$
# rather than hand-writing 9 desc branches of its own.
#
# VERIFICATION FLAG: every confirmed type = location example in base game
# (this folder's own derry/london) is a display-name OVERRIDE for one
# specific, already-named location entry - auto-substituted wherever that
# location's GetName renders - not a general "evaluate this concept at an
# arbitrary scope from inside unrelated text" mechanism, unlike the
# confirmed type = country examples. No confirmed base-game precedent was
# found for a $name$-style concept being scope-qualified to something
# other than the implicit default (root) from inside event desc text.
# Before shipping, force-fire z_aqd_jfatwa.1 / z_aqd_fatwa.1 and confirm
# $aqd_seat_madhhab_name$ actually resolves against scope:target_location
# (the seat) and not root - check the rendered text and error.log. If it
# resolves against the wrong scope, fall back to 9 concrete desc-key
# branches per event instead; nothing else in either fatwa system changes.
# =====================================================================

aqd_seat_madhhab_name = {
	type = location

	text = {
		localization_key = aqd_seat_madhhab_name_hanafi
		trigger = { has_building = building_type:seat_of_hanafi_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_hanbali
		trigger = { has_building = building_type:seat_of_hanbali_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_maliki
		trigger = { has_building = building_type:seat_of_maliki_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_shafii
		trigger = { has_building = building_type:seat_of_shafii_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_zahiri
		trigger = { has_building = building_type:seat_of_zahiri_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_ismaili
		trigger = { has_building = building_type:seat_of_ismaili_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_jafari
		trigger = { has_building = building_type:seat_of_jafari_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_zaidi
		trigger = { has_building = building_type:seat_of_zaidi_jurists }
	}
	text = {
		localization_key = aqd_seat_madhhab_name_ibadi
		trigger = { has_building = building_type:seat_of_ibadi_jurists }
	}
}
